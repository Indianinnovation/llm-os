"""LLM OS — a private, local-first agentic kernel.

A small language model acts as the intent router; deterministic,
sandboxed tools do the work. Nothing leaves the machine.
"""

__version__ = "0.1.0"
