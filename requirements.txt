# Pinned to the exact versions this release is tested against. A project that
# pins model digests and MCP server hashes must pin its own runtime too:
# two installs a week apart get the same code, or the supply-chain claim is
# false at the layer an auditor checks first. Full transitive closure in
# requirements.lock (pip install -r requirements.lock for a byte-identical env).
fastapi==0.139.0
uvicorn==0.51.0
pydantic==2.13.4
ollama==0.6.2
mcp==1.28.1
chromadb==1.5.9
requests==2.34.2
streamlit==1.59.1
